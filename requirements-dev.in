-r requirements.in
# >=9.0.3 required: 8.4.2 (and earlier 8.x) is affected by PYSEC-2026-1845,
# found by pip-audit against this lockfile.
pytest>=9.0.3,<10
# Pinned exactly: a newer ruff release broadened its own default rule set
# (confirmed 0.16.4 vs the 0.11.13 this was validated against) and turned a
# clean lint into 58 failures with no code change on our side. pyproject.toml
# also pins an explicit `select` so this can't recur even on a bump.
ruff==0.16.5
hypothesis>=6.100,<7
